---
title: Interface & Plugins
description: Commands for security, notifications, appearance and plugins.
---

import IpcCommand from '../../../../components/IpcCommand.astro';

Customize and secure your desktop experience with these commands.

### Security & Privacy

| Function | Command | Description |
|----------|---------|-------------|
| **Lock Screen** | <IpcCommand command="lockScreen lock" /> | Lock your screen |
| **Idle Inhibit** | <IpcCommand command="idleInhibitor toggle" /> | Toggle idle inhibitor |
| **Enable Inhibit** | <IpcCommand command="idleInhibitor enable" /> | Enable idle inhibitor |
| **Disable Inhibit** | <IpcCommand command="idleInhibitor disable" /> | Disable idle inhibitor |

### Notifications

| Function | Command | Description |
|----------|---------|-------------|
| **History** | <IpcCommand command="notifications toggleHistory" /> | View past notifications |
| **Get History** | <IpcCommand command="notifications getHistory" /> | Get history as JSON |
| **Do Not Disturb** | <IpcCommand command="notifications toggleDND" /> | Toggle silence mode |
| **Enable DND** | <IpcCommand command="notifications enableDND" /> | Enable silence mode |
| **Disable DND** | <IpcCommand command="notifications disableDND" /> | Disable silence mode |
| **Clear All** | <IpcCommand command="notifications clear" /> | Clear notification history |
| **Remove Oldest** | <IpcCommand command="notifications removeOldestHistory" /> | Remove oldest from history |
| **Remove ID** | <IpcCommand command="notifications removeFromHistory $id" /> | Remove notification by ID |
| **Dismiss Oldest** | <IpcCommand command="notifications dismissOldest" /> | Dismiss oldest active |
| **Dismiss All** | <IpcCommand command="notifications dismissAll" /> | Dismiss active notifications |

### Toast Notifications

| Function | Command | Description |
|----------|---------|-------------|
| **Send Toast** | <IpcCommand command="toast send '$json'" /> | Display a toast notification |

The toast `send` command accepts a JSON object with the following fields:

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `title` | string | Yes | Toast title |
| `body` | string | No | Toast body/description |
| `icon` | string | No | Icon name (only for notice type) |
| `type` | string | No | `notice` (default), `warning`, or `error` |
| `duration` | number | No | Duration in ms (defaults: notice=3000, warning=4000, error=6000) |

**Examples:**
```bash
# Simple notice
qs -c noctalia-shell ipc call toast send '{"title": "Recording", "body": "Started (AV1 HDR)", "icon": "media-record"}'

# Warning toast
qs -c noctalia-shell ipc call toast send '{"title": "Low battery", "type": "warning"}'

# Error with custom duration
qs -c noctalia-shell ipc call toast send '{"title": "Connection failed", "body": "Check your network", "type": "error", "duration": 10000}'
```
### Appearance

#### Visibility

| Function | Command | Description |
|----------|---------|-------------|
| **Toggle Bar** | <IpcCommand command="bar toggle" /> | Toggle bar visibility |
| **Show Bar** | <IpcCommand command="bar show" /> | Show bar |
| **Hide Bar** | <IpcCommand command="bar hide" /> | Hide bar |
| **Toggle Dock** | <IpcCommand command="dock toggle" /> | Toggle dock visibility |
| **Show Widgets** | <IpcCommand command="desktopWidgets enable" /> | Enable desktop widgets |
| **Hide Widgets** | <IpcCommand command="desktopWidgets disable" /> | Disable desktop widgets |
| **Toggle Widgets** | <IpcCommand command="desktopWidgets toggle" /> | Toggle desktop widgets |
| **Edit Widgets** | <IpcCommand command="desktopWidgets edit" /> | Toggle widget editing |

#### Theme Controls

| Function | Command | Description |
|----------|---------|-------------|
| **Dark Mode** | <IpcCommand command="darkMode toggle" /> | Toggle system theme |
| **Set Dark** | <IpcCommand command="darkMode setDark" /> | Force dark theme |
| **Set Light** | <IpcCommand command="darkMode setLight" /> | Force light theme |
| **Color Scheme** | <IpcCommand command="colorScheme set <theme>" /> | Set scheme by name |

#### Wallpaper Management

| Function | Command | Description |
|----------|---------|-------------|
| **Selector** | <IpcCommand command="wallpaper toggle" /> | Toggle wallpaper selector |
| **Set Wallpaper** | <IpcCommand command="wallpaper set $path $monitor" /> | Set wallpaper on monitor |
| **Random** | <IpcCommand command="wallpaper random" /> | Apply random wallpaper |
| **Toggle Auto** | <IpcCommand command="wallpaper toggleAutomation" /> | Toggle automation |
| **Enable Auto** | <IpcCommand command="wallpaper enableAutomation" /> | Enable automation |
| **Disable Auto** | <IpcCommand command="wallpaper disableAutomation" /> | Disable automation |

:::note[Wallpaper variables]
- `$path`: Full path to your wallpaper image file
- `$monitor`: Monitor identifier
:::

### Plugin Controls

| Function | Command | Description |
|----------|---------|-------------|
| **Settings** | <IpcCommand command="plugin openSettings $pluginId" /> | Open plugin settings |
| **Open Panel** | <IpcCommand command="plugin openPanel $pluginId" /> | Open plugin panel |
| **Close Panel** | <IpcCommand command="plugin closePanel $pluginId" /> | Close plugin panel |
| **Toggle Panel** | <IpcCommand command="plugin togglePanel $pluginId" /> | Toggle plugin panel |

:::note[Plugin ID]
Replace `$pluginId` with the actual plugin identifier (e.g., `todo`, `pomodoro`).
:::

### Misc

| Function | Command | Description |
|----------|---------|-------------|
| **State** | <IpcCommand command="state all" /> | Output settings state |
| **Set Location** | <IpcCommand command="location set $name" /> | Set geographic location |
| **Get Location** | <IpcCommand command="location get" /> | Get geographic location |
